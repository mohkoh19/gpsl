# @package _global_

defaults:
  - override /data: cifar10
  - override /model: gpsl
  - override /distributed: gpsl

tags: ["cifar10", "resnet18", "gpsl"]

trainer:
  min_epochs: 100
  max_epochs: 100

distributed:
  subset_config:
    distribution: "non_iid"
    params:
      C: 2
      alpha: 3