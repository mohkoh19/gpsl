_target_: src.data.gpsl_distributed_datamodule.DistributedDataModule
num_clients: 128
subset_config:
  distribution: "non_iid"
  params:
    C: 2
    alpha: 3

sampler:
  # _target_: "src.utils.sampling.UniformGlobalSampler"
  _target_: "src.utils.sampling.LocalFixedSamplerNonProportional"
  _partial_: true